# Minimal Hyprland config for terminal/IDE/browser workspaces
$mainMod = SUPER
$terminal = ghostty

# Monitor setup
monitor=,preferred,auto,1
windowrule = fullscreen, class:firefox, workspace:browser
exec-once = hyprctl dispatch workspace name:cli
#exec-once = [workspace name:cli silent] ghostty
exec-once = hyprpaper
exec-once = waybar

# Key bindings
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, D, exec, wofi --show drun,
bind = $mainMod, W, killactive,
bind = $mainMod, Q, exit,

# Workspace switching
workspace = name:cli, persistent:true
workspace = name:dev, persistent:true
workspace = name:web, persistent:true
bind = $mainMod, 1, workspace, name:cli
bind = $mainMod, 2, workspace, name:dev
bind = $mainMod, 3, workspace, name:web
bindm = $mainMod, mouse:272, movewindow

# Window management
bind = $mainMod, F, fullscreen, 0
bind = $mainMod, Space, togglefloating,

# Minimal visual settings
decoration {
    rounding = 0
}

animations {
    enabled = true
    workspace_wraparound = true
}

general {
    gaps_in = 2
    gaps_out = 8
    border_size = 2
    col.active_border = rgba(140,170,238,0.9)
    col.inactive_border = rgba(0,0,0,1)
    resize_on_border = true
}

gestures {
    workspace_swipe_forever = true
    gesture = 3, horizontal, workspace
}

misc {
    animate_manual_resizes = true
    animate_mouse_windowdragging = true
    enable_swallow = false
}

cursor {
    persistent_warps = true
    warp_on_change_workspace = 1
}
